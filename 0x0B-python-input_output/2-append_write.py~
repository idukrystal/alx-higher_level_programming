#!/usr/bin/python3
"""module inputs  contents to  a file"""


def write_file(filename="", text=""):
    """inputss contents of filename"""
    with open(filename, 'w', encoding="utf-8") as fil:
        written = fil.write(text)
    return written
